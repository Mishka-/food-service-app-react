import React, { Component, PropTypes } from 'react';

const EmptyCart = () => {
  return (
    <div className="cart">
      <div className="no-items">
        Your cart is empty
      </div>
    </div>
  );
};

export default EmptyCart;
